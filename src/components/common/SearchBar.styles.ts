// src/components/common/SearchBar.styles.ts
import styled from 'styled-components';

export const SearchInput = styled.input`
  padding: 8px;
  border-radius: 4px;
  border: 1px solid #ccc;
  width: calc(90% - 15px);
  // max-width: 400px;
  margin:0 auto;
  outline: none;
      position: absolute;
    top: 60px;
    right: 15px;

`;
